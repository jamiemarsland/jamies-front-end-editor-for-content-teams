# WordPress.org listing assets (icon, banner, screenshots, blueprint) — synced to
# the SVN assets/ directory on release, never shipped inside the plugin zip.
.wordpress-org
.wordpress-org/**

# Don't ship the ignore file itself.
.pressshipignore

# Repo-only dev docs — keep in git, never ship to WordPress.org.
RELEASE.md
GITHUB-RELEASE-NOTES.md

# Demo-only seeder (used via the blueprint), never shipped.
demo
demo/**
